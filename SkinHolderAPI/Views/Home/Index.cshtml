@{
    Layout = null;
}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <title>Private API</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <style>
        body {
            font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", sans-serif;
            background-color: #0e1117;
            color: #c9d1d9;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            padding: 2rem;
            text-align: center;
            min-height: 100vh;
            margin: 0;
        }

        h1 {
            font-size: 2.5rem;
            margin-bottom: 1rem;
        }

        p {
            font-size: 1.2rem;
            color: #8b949e;
            margin: 0.5rem 0;
        }

        a {
            display: inline-block;
            margin-top: 1rem;
            padding: 0.6rem 1.2rem;
            font-size: 1rem;
            color: #ffffff;
            background-color: #238636;
            text-decoration: none;
            border-radius: 6px;
            transition: background-color 0.2s ease-in-out;
        }

        a:hover {
            background-color: #2ea043;
        }

        @@media (max-width: 600px) {
            h1 {
                font-size: 2rem;
            }

            p {
                font-size: 1rem;
            }

            a {
                font-size: 0.95rem;
                padding: 0.5rem 1rem;
            }
        }
    </style>
</head>
<body>
    <h1>Private API for SkinHolder</h1>
    <p>This API is not intended for public use at the moment, so no documentation is available.</p>
    <p>
        Learn more about SkinHolder at
        <a href="https://skinholder.jagoba.dev/about" target="_blank" rel="noopener noreferrer">skinholder.jagoba.dev/about</a>
    </p>
</body>
</html>
